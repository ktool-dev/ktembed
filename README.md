# KtEmbed

> Embed static resources directly into your Kotlin binaries

KtEmbed is a Kotlin Multiplatform library + Gradle plugin that lets you embed static resources (text, JSON, HTML,
images, etc.) directly into your compiled applications.
